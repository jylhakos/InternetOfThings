version: '3.8'

services:
  php-api:
    build:
      target: development
    environment:
      - APP_ENV=development
      - XDEBUG_MODE=debug
      - XDEBUG_CONFIG=client_host=host.docker.internal
    volumes:
      - .:/var/www/html
    ports:
      - "9003:9003" # Xdebug port